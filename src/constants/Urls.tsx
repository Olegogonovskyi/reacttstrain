const baseUrl = 'http://owu.linkpc.net/carsAPI/v2'

const authUrls = {
    auth: '/auth',
    refresh: '/auth/refresh'
}

const carsUrls = {
    getAllcars: '/cars'
}

export {
    baseUrl, authUrls, carsUrls
}